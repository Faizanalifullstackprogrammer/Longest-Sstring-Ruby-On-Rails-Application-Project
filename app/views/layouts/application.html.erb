<!DOCTYPE html>
<html>
  <head>
    <title>LongestStringWebapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
      <div class="container-fluid">
        <div class="container-fluid d-flex justify-content-end align-items-center">
        <%= link_to 'Home', root_path, class: 'navbar-brand' %>
         <%= nav_signup %>
        </div>
      </div>
    </nav>
    </header>

    <% if flash[:notice] %>
      <div id="successMessage" class="p-0 m-0 d-flex align-items-center justify-content-center">
          <p class="notice" id="notice"><%= notice %></p> 
      </div>
    <% end %>
    
    <% if flash[:alert] %>
      <div id="successMessage" class="p-2 d-flex align-items-center justify-content-center">
        <p class="alert" id="alert" ><%= alert %></p>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
